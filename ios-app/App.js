/**
 * CYPHR MESSENGER iOS - ENTRY POINT
 * 
 * Enterprise-grade post-quantum secure messenger for iOS
 * Imports adapted App.jsx with all original functionality
 */

import { registerRootComponent } from 'expo';
import App from './src/App';

// Register the main app component
registerRootComponent(App);

export default App;